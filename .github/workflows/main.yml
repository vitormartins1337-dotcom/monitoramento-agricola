name: Consultoria Agro-Intel (Versao Estavel)

on:
  schedule:
    - cron: '0 9 * * *' # 06:00 em Ibicoara
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout do código
      uses: actions/checkout@v3

    - name: Configurar Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Instalar dependências
      run: |
        python -m pip install --upgrade pip
        pip install requests

    - name: Executar Script Profissional
      env:
        OPENWEATHER_KEY: ${{ secrets.OPENWEATHER_KEY }}
        GMAIL_PASSWORD: ${{ secrets.GMAIL_PASSWORD }}
      run: python clima_alerta.py

    - name: Salvar Histórico e Limpar Input
      run: |
        git config --global user.name "AgroRobot-Ibicoara"
        git config --global user.email "agro-robot@github.com"
        git add input_atividades.txt caderno_de_campo_master.csv
        git commit -m "📝 Registro Profissional Diário" || echo "Sem alterações"
        git push
